# Intentionally left without VITE_API_URL.
#
# API calls now go through a same-origin proxy (functions/api/[[path]].js)
# instead of pointing directly at the tunnel URL. That means this file no
# longer needs updating when the tunnel restarts, only the BACKEND_URL
# secret on the Pages project does. See README for the one-line command.
